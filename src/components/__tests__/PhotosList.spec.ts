//TODO:
// [] - should be displayed a stub component if list is empty
// [] - should be displayed a correct count of the posts, depending on the response model
// [] - should be filtered by a search term
// [] - should be displayed limited count of the posts
// [] - might be able to add next photos list depending on limit

import { render, screen } from '@testing-library/vue';
import { expect, describe, it } from 'vitest';
import PhotosList from '@/components/PhotosList.vue';

describe('PhotosList component', () => {});
